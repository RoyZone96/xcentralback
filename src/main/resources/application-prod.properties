# Production configuration

# Database configuration (PostgreSQL for Supabase) - Using Supabase Pooler
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require&connectTimeout=60&socketTimeout=60&loginTimeout=60}
spring.datasource.username=${DB_USERNAME:postgres.dzjxerwuoieoqxvpphky}
spring.datasource.password=${DB_PASSWORD:RnHPJz2AJZFCnjc8}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server configuration
server.port=${PORT:8080}

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.xcentral=INFO

# File upload - disable for production (use cloud storage instead)
spring.servlet.multipart.enabled=false

# CORS configuration - will be overridden by environment variables in production
spring.mvc.cors.allowed-origins=${CORS_ORIGINS:https://xcentralfront-66xkgz8dx-royzone96s-projects.vercel.app,https://xcentralfront.vercel.app}
spring.mvc.cors.allowed-methods=GET, POST, PUT, DELETE, OPTIONS

# Mail configuration - will be overridden by environment variables in production
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:xcentralmail@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ncbnypvkjfwgxvoq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application URL - for generating links in emails
app.base-url=${APP_BASE_URL:https://xcentralback.onrender.com}
app.frontend-url=${APP_FRONTEND_URL:https://xcentralfront-ciywa0xn1-royzone96s-projects.vercel.app,https://xcentralfront-66xkgz8dx-royzone96s-projects.vercel.app,https://xcentralfront.vercel.app}

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey}
jwt.expiration=${JWT_EXPIRATION:86400}
