spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
spring.application.name=xcentralback

# Database configuration - PostgreSQL Supabase Pooler Connection
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require&connectTimeout=60&socketTimeout=60&loginTimeout=60
spring.datasource.username=postgres.dzjxerwuoieoqxvpphky
spring.datasource.password=RnHPJz2AJZFCnjc8
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Temporarily disable JPA initialization to test connection
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

# Connection pool settings with more aggressive timeouts
spring.datasource.hikari.connection-timeout=120000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# Debug logging
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG

# CORS configuration - will be overridden by environment variables in production
spring.mvc.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}
spring.mvc.cors.allowed-methods=GET, POST, PUT, DELETE, OPTIONS

# Mail configuration - will be overridden by environment variables in production
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:xcentralmail@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ncbnypvkjfwgxvoq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.test-connection=false
logging.level.org.springframework.mail=DEBUG

# Courier configuration
courier.auth-token=${COURIER_AUTH_TOKEN:}

# Application URL - for generating links in emails
app.base-url=${APP_BASE_URL:http://localhost:8080}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:3000}

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey}
jwt.expiration=${JWT_EXPIRATION:86400}

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
